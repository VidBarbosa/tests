{
  "name": "test-project",
  "version": "1.0.0",
  "description": "Backend API for managing users, projects, rates and tasks.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint . --ext .ts",
    "seed": "ts-node src/seeds/seed.ts",
    "db:reset": "ts-node src/scripts/reset-db.ts"
  },
  "dependencies": {
    "@fastify/swagger": "^8.0.0",
    "@fastify/swagger-ui": "^1.0.0",
    "dotenv": "^16.0.0",
    "fastify": "^4.0.0",
    "pg": "^8.0.0",
    "pino": "^9.0.0",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "pino-pretty": "^13.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
